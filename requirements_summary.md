# 5孔ピトー管較正Gコード生成ソフト 要求概要

## 目的

4軸ステッピングモーター装置で5孔ピトー管を風洞中心へ配置し、指定したAoA・AoSの較正点を走査するGRBL用Gコードを生成する。

## 装置と軸構成

| GRBL軸 | 動作 | 単位 |
|---|---|---|
| X | 水平方向並進 | mm |
| Y | 上下方向並進 | mm |
| Z | 実ピッチ軸。Z軸周りの回転 | deg |
| A | 実ロール軸。ピトー管軸周りの回転 | deg |

基準姿勢では全軸を0とする。XY原点は可動範囲中央かつ風洞中心、ZA原点はAoA=AoS=0 degとする。

## GUI入力

- AoA範囲、AoS範囲
- AoA点数、AoS点数（両端を含む）
- ピッチ回転中心から先端までのX/Y距離
- 較正点保持時間 [s]
- 合成送り速度 Feed rate F [unit/min]
- X/Y/Z/Aの可動範囲
- 初期化Gコードのテキストファイル

送り速度はGRBLのG94における送り速度として扱う。X/Y/Z/Aの個別速度、加速度、steps/mm、steps/degはGUIで扱わず、GRBL/CNC.js側で設定する。

## オプション

- 蛇行走査を使用する
- Gコードコメントを出力する

## 主要な処理

各較正点について、入力AoA/AoSを再現する実ピッチ角と実ロール角を算出する。

AoA/AoSの範囲または点数が変更された場合、入力値が有効なら較正点列と実軸指令値をリアルタイムで自動再生成する。

入力値エラーはモーダルダイアログで表示せず、該当入力フィールドの色や枠を変え、フィールド近傍または固定メッセージ領域に理由を表示する。生成禁止エラーがある間はシミュレーションおよびGコード生成を無効化する。

ピッチ回転によるピトー管先端の位置ずれはX/Y並進で補正する。X/Yが可動範囲を超える場合は範囲端に飽和させて警告し、シミュレーションおよびGコード生成を継続する。Z/Aが可動範囲を超える場合は飽和させず、シミュレーションおよびGコード生成を禁止する。

較正はAoA方向の移動回数を減らし、AoS方向をスイープする。蛇行走査を選択した場合はAoA行ごとにAoS走査方向を反転する。

## 出力

- `.nc`形式のGコード
- Gコードの先頭に初期化Gコード、`$H`、`G21`、`G90`、`G94`を出力
- 各較正点でX/Y/Z/Aを同時指令し、入力された合成送り速度を`F`ワードで指定
- 移動指令は`G01`、保持指令は`G04 P<秒>`としてG番号を2桁ゼロ埋め表記
- 最終較正点で停止

## GUI・シミュレーション

GUIは日本語とする。AoA/AoSの2次元較正点マップを表示し、X/Y飽和点とZ/A範囲超過点は識別できる表示とする。

シミュレーションは任意実行とし、生成禁止エラーがなければ実行していなくてもGコード生成可能とする。再生は保持時間を再現せず、全体を約10秒で表示する。表示は横面図（ピッチ、X、Y）と正面図（ロール）に分ける。
