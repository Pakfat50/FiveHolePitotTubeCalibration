# 5孔ピトー管較正Gコード生成ソフト 要求概要

## 目的

4軸ステッピングモーター装置で5孔ピトー管を風洞中心へ配置し、指定したAoA・AoSの較正点を走査するGRBL用Gコードを生成する。

## 装置と軸構成

| GRBL軸 | 動作 | 単位 |
|---|---|---|
| X | 水平方向並進 | mm |
| Y | 上下方向並進 | mm |
| Z | 実ピッチ軸。Z軸周りの回転 | deg |
| A | 実ロール軸。ピトー管軸周りの回転 | deg |

基準姿勢では全軸を0とする。XY原点は可動範囲中央かつ風洞中心、ZA原点はAoA=AoS=0 degとする。

## GUI入力

- AoA範囲、AoS範囲
- AoA点数、AoS点数（両端を含む）
- ピッチ回転中心から先端までのX/Y距離
- 較正点保持時間 [s]
- X/Y速度 [mm/s]
- Z/A速度 [deg/s]
- X/Y/Z/Aの可動範囲
- 初期化Gコードのテキストファイル

加速度、steps/mm、steps/degはGUIで扱わず、GRBL/CNC.js側で設定する。

## オプション

- 正確なAoA/AoSで較正する
- 実軸移動量を最小化する
- 蛇行走査を使用する
- Gコードコメントを出力する

## 主要な処理

正確モードでは、入力AoA/AoSを実現するように実ピッチ・実ロールへ変換する。非正確モードでは、入力AoA/AoSを実ピッチ・実ロールへ直接対応させ、変換後の実AoA/AoSを表示する。

ピッチ回転によるピトー管先端の位置ずれはX/Y並進で補正する。可動範囲を超える場合は軸指令値を飽和させ、先端の理想位置からのX/Y逸脱量を警告する。飽和点でも較正は継続する。

較正はAoA方向の移動回数を減らし、AoS方向をスイープする。蛇行走査と実軸移動量最小化はオプションで選択する。

## 制限事項

AoAまたはAoSが±30 degを超える場合は警告を表示し、シミュレーションとGコード生成を禁止する。

## 出力

- `.nc`形式のGコード
- Gコードの先頭に初期化Gコード、`$H`、`G21`、`G90`、`G94`を出力
- 各較正点でX/Y/Z/Aを同時指令
- 保持時間は`G4 P<秒>`で出力
- 最終較正点で停止

## GUI・シミュレーション

GUIは日本語とする。AoA/AoSの2次元較正点マップを表示し、飽和点は色分けする。

シミュレーションは任意実行とし、実行しなくてもGコード生成可能とする。再生は保持時間を再現せず、全体を約10秒で表示する。表示は横面図（ピッチ、X、Y）と正面図（ロール）に分ける。
